import QtQuick
import QtQuick.Controls

Menu {
    background: Rectangle {
        implicitWidth: 220
        implicitHeight: 40
        color: sysPalette.button
        border.color: sysPalette.mid
        radius: 2
    }
}
